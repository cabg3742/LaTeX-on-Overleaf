\lstinputlisting[%
    language    = LaTeX,
    firstline   = {127},
    lastline    = {128},
    firstnumber = {127},
    caption     = {[Date and time settings]Date and time settings.},
    label       = {lst:tutorial/preamble/datetime}
]{tools/settings.tex}

The \texttt{datetime2} \gls{package} provides \glspl{macro} to format dates, times, and time zones. The \gls{command} on the second line makes sure we follow the \glsname{ISO} 8601 norm for display of date and time inside the text (same constraint as in \S~\ref{sec:tutorial/preamble/bib}).